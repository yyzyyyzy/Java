public class Array1Demo {
    public static void main(String[] args) {
        /*
        数组的动态初始化：手动指定数组的长度，由系统给出默认的初始值
        对于 boolean 类型的数组，默认值为 false。
        对于数字类型（如 byte、short、int、long、float 和 double）的数组，默认值为 0 或 0.0。
        对于 char 类型的数组，默认值为 '\u0000'（即空字符）。
        对于对象类型（如 String、Object 等）的数组，默认值为 null。
         */
        int[] myArray = new int[5];
    }
}